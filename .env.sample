# Copy this file to .env and fill in the values you need for your provider(s).
# The CLI automatically loads .env via python-dotenv.
#
# You can configure one or multiple providers at the same time.
# Backends: OpenAI Sora-2, Azure OpenAI (Sora-2), Google Veo-3 (Vertex AI), RunwayML (Gen-4 & Veo)

#############################
# OpenAI Sora-2 (Direct API)
#############################
# REQUIRED
# Get your key from https://platform.openai.com/
OPENAI_API_KEY=

# OPTIONAL
# (No additional env vars required for Sora-2 in this project)


######################################
# Azure AI Foundry (Sora-2 on Azure)
######################################
# REQUIRED
# Get these from https://ai.azure.com/
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT= # e.g., https://your-resource.openai.azure.com/

# OPTIONAL
# API version defaults to 2024-10-01-preview if not set
AZURE_OPENAI_API_VERSION=2024-10-01-preview


#############################
# Google Veo-3 (Vertex AI)
#############################
# REQUIRED (when using the Veo-3 provider)
# Project ID is required because Veo models are only available via Vertex AI
GOOGLE_CLOUD_PROJECT=

# Access token used as a Bearer token. Two common ways to set this:
# 1) gcloud (recommended):
#    export GOOGLE_API_KEY="$(gcloud auth application-default print-access-token)"
# 2) Browser OAuth via CLI flags:
#    Use --google-login or --google-login-browser; the token is set at runtime.
#    You can optionally paste a token here to avoid the login step.
GOOGLE_API_KEY=

# OPTIONAL
# Vertex AI location (default: us-central1)
GOOGLE_CLOUD_LOCATION=us-central1
# Alternative env names (recognized by availability checks):
# VEO3_API_KEY=            # alternative to GOOGLE_API_KEY
# VEO3_LOCATION=           # alternative to GOOGLE_CLOUD_LOCATION
# GOOGLE_APPLICATION_CREDENTIALS= # path to a service account JSON (not used directly by the Veo client)

# Notes:
# - If your OAuth2 token expires during a run, the client will attempt to re-authenticate
#   automatically (via gcloud or browser OAuth, if previously set up) and retry.
# - For browser OAuth, place client_secrets.json in video_gen/providers/google_provider/.
#   Cached credentials are stored as token.pickle in the same folder.


#########################
# RunwayML (Gen-4 & Veo)
#########################
# REQUIRED
RUNWAY_API_KEY=

# OPTIONAL
# Default model used by the Runway provider (gen4_turbo, gen4, veo3, veo3.1, veo3.1_fast)
RUNWAY_MODEL=gen4_turbo
# Override the Runway API base URL if needed (defaults to the dev endpoint below)
RUNWAY_BASE_URL=https://api.dev.runwayml.com/v1


#############################
# General notes
#############################
# - Only set the variables for the providers you plan to use.
# - The CLI prints which providers are available based on the variables you set.
# - You can always pass --list-models to see models and environment hints.
# - Stitching mode is supported on Veo 3.1 and Runway Veo models.
